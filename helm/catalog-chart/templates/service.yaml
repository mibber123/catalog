apiVersion: v1
kind: Service
metadata:
  name: {{ include "catalog-chart.fullname" . }}
  labels:
    {{- include "catalog-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "catalog-chart.selectorLabels" . | nindent 4 }}
